{
  "name": "domain-suppliers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "domains/suppliers",
  "projectType": "library",
  "tags": ["scope:domain", "domain:suppliers"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/domains/suppliers",
        "main": "domains/suppliers/index.ts",
        "tsConfig": "domains/suppliers/tsconfig.lib.json"
      }
    }
  }
}

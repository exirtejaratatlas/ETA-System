# Setup

ETA-System is in early implementation (Phase 0/1 — see [ADR-0001](../decisions/ADR-0001-eta-system-target-architecture.md)). There is no build/run tooling yet: no `package.json`, no lockfile, no CI. This file will be filled in as Phase 1 tooling (Nx workspace, per [ADR-0002](../decisions/ADR-0002-monorepo-tooling-and-runtime-split.md)) is actually established.

Do not assume any build/lint/test command exists until this file says otherwise.

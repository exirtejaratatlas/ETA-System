{
  "name": "events",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "platform/events/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:events"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/platform/events",
        "main": "platform/events/src/index.ts",
        "tsConfig": "platform/events/tsconfig.lib.json"
      }
    }
  }
}

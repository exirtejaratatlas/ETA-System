# platform/plugins

Plugin SDK, manifest schema, and sandboxed runtime/registry. Permission model shares one authorization primitive with ai/mcp-registry (ADR-0007) rather than maintaining a second, parallel scheme.
